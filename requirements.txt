# FastAPI 및 서버 관련
fastapi==0.119.1            # 비동기 Python 웹 프레임워크
uvicorn[standard]==0.38.0   # ASGI 서버 (FastAPI 실행용)
watchfiles==1.1.1           # 코드 변경 감지 (uvicorn --reload 시 사용)
httptools==0.7.1            # 빠른 HTTP 파서
websockets==15.0.1          # WebSocket 지원
h11==0.16.0                 # HTTP/1.1 프로토콜 구현
anyio==4.11.0               # 비동기 I/O 추상화 레이어
sniffio==1.3.1              # anyio용 백엔드 감지
starlette==0.48.0           # FastAPI의 기반 ASGI 프레임워크

# ORM 및 데이터베이스 관련
tortoise-orm==0.25.1        # 비동기 ORM
asyncpg==0.30.0             # PostgreSQL 비동기 드라이버
aiosqlite==0.21.0           # SQLite 비동기 드라이버
pypika-tortoise==0.6.2      # ORM용 SQL 빌더
aerich==0.9.2               # Tortoise ORM 마이그레이션 툴

# 환경 설정 / 유틸리티
python-dotenv==1.1.1        # .env 환경변수 로드
click==8.3.0                # CLI 명령어 지원
asyncclick==8.3.0.7         # 비동기 CLI 도구
colorama==0.4.6             # 터미널 컬러 출력
dictdiffer==0.9.0           # 딕셔너리 비교 도구
PyYAML==6.0.3               # YAML 파일 파서
iso8601==2.1.0              # ISO 날짜 파서
pytz==2025.2                # 시간대 라이브러리
idna==3.11                  # 국제 도메인 지원

# 인증 / 보안
passlib[bcrypt]             # 비밀번호 해시 (bcrypt 포함)
python-jose[cryptography]   # JWT 토큰 인코딩/디코딩

# 데이터 검증 / 타입
pydantic==2.12.3            # 데이터 검증 및 설정 관리
pydantic_core==2.41.4       # Pydantic 핵심 모듈
annotated-types==0.7.0      # 타입 힌트용 어노테이션 지원
typing_extensions==4.15.0   # 최신 타입 지원 (Python 버전 보완)
typing-inspection==0.4.2    # 타입 분석용 유틸리티